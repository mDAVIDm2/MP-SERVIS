import { OnApplicationBootstrap } from '@nestjs/common';
import { Repository } from 'typeorm';
import { Organization } from '../organizations/organization.entity';
import { StaffMember } from '../organizations/staff-member.entity';
import { MasterSchedule } from '../organizations/master-schedule.entity';
import { OrganizationSettings } from '../organizations/organization-settings.entity';
import { User } from '../users/user.entity';
import { Order } from '../orders/order.entity';
import { OrderItem } from '../orders/order-item.entity';
import { Chat } from '../chats/chat.entity';
import { ChatMessage } from '../chats/chat-message.entity';
import { CarBrand } from '../reference/car-brand.entity';
import { CarModel } from '../reference/car-model.entity';
import { CarGeneration } from '../reference/car-generation.entity';
export declare class SeedService implements OnApplicationBootstrap {
    private orgRepo;
    private staffRepo;
    private scheduleRepo;
    private settingsRepo;
    private userRepo;
    private orderRepo;
    private itemRepo;
    private chatRepo;
    private messageRepo;
    private brandRepo;
    private modelRepo;
    private generationRepo;
    constructor(orgRepo: Repository<Organization>, staffRepo: Repository<StaffMember>, scheduleRepo: Repository<MasterSchedule>, settingsRepo: Repository<OrganizationSettings>, userRepo: Repository<User>, orderRepo: Repository<Order>, itemRepo: Repository<OrderItem>, chatRepo: Repository<Chat>, messageRepo: Repository<ChatMessage>, brandRepo: Repository<CarBrand>, modelRepo: Repository<CarModel>, generationRepo: Repository<CarGeneration>);
    onApplicationBootstrap(): Promise<void>;
    run(): Promise<void>;
    private seedCarBrandsAndModels;
    private seedCarGenerations;
}
